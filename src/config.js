// ==============================================================
// Written by steve saxton <steves@codeuniquely.co.uk>
// various configuration optionsc - not yet used
// ==============================================================

/* eslint-disable no-unused-labels */
var config = {
  maxEntries: 10,
};

/* eslint-enable no-unused-labels */

export default config;